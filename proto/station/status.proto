edition = "2024";

package station;

/* 广播电台的播出状态
 *
 * 广播电台存在三种播出状态，广播中状态，每个月底的停机检修以及该电台停播（哦不！）
 */
enum StationStatus {
  // 广播中
  ONAIR       = 0;

  // 停机检修
  MAINTAINING = 1;

  // 电台停播
  OFFAIR      = 2;
}
